name: latexplotlib
channels:
  - conda-forge
  - defaults
dependencies:
  - coverage
  - ipython
  - matplotlib
  - pip
  - pre-commit
  - pytest
  - pytest-cov
  - pytest-mock
  - python>=3.8
  - ruff
  - pip:
      - python-lsp-server
      - pylsp-mypy
      - "--editable=.[tests]"
